#pragma once

#include <nlohmann/json.hpp>

#include "api/connection.hpp"
#include "api/target.hpp"

namespace warmane
{
	class character
	{
	public:

	private:

	};

	//character load_character(api::connection& connection, const api::target& target)
	//{

	//}
}
